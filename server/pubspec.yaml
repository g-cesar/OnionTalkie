name: onion_talkie_server
description: Local bridge server for OnionTalkie web — routes browser WebSocket to Tor.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_static: ^1.1.0
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^3.0.0
  args: ^2.4.0

dev_dependencies:
  lints: ^5.1.0
